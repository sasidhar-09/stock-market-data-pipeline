name: 'stock_dbt'
version: '1.0.0'
config-version: 2

profile: 'stock_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  stock_dbt:
    # Staging models (Silver layer)
    staging:
      +materialized: view
      +schema: staging
    
    # Marts models (Gold layer)
    marts:
      +materialized: table
      +schema: marts